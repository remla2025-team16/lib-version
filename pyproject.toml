[build-system]
requires = ["setuptools>=42", "wheel", "setuptools_scm"]
build-backend = "setuptools.build_meta"

[project]
name = "lib-version"
version = "0.0.8"
dependencies = [
    "gitpython==3.1.32"
]

[tool.setuptools]
packages = ["lib_version"]